# Optional: Enable HTTP Basic Auth when both are set
# BASIC_AUTH_USERNAME=admin
# BASIC_AUTH_PASSWORD=changeme

# Anthropic API key for AI-generated headline narratives (optional).
# Without this key, the headline page falls back to hardcoded template text.
# ANTHROPIC_API_KEY=sk-ant-...
